<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MAProject"
        tools:targetApi="31">
        <activity
            android:name=".ShopActivity"
            android:exported="false" />
        <!-- Login Activity -->
        <activity
            android:name=".ui.auth.LoginActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- Register Activity -->
        <activity
            android:name=".ui.auth.RegisterActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" /> <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:launchMode="singleTop" /> <!-- Statistics Activity -->
        <activity
            android:name=".ui.statistics.StatisticsActivity"
            android:exported="false" /> <!-- Profile Activity -->
        <activity
            android:name=".ui.model.ProfileActivity"
            android:exported="false" /> <!-- Change Password Activity -->
        <activity
            android:name=".ui.auth.ChangePasswordActivity"
            android:exported="false" /> <!-- Friends Activity -->
        <activity
            android:name=".ui.friends.FriendsActivity"
            android:exported="false" /> <!-- Alliance Activities -->
        <activity
            android:name=".ui.alliance.AllianceActivity"
            android:exported="false" />
        <activity
            android:name=".ui.alliance.AllianceChatActivity"
            android:exported="false" />
        <activity
            android:name=".ui.alliance.CreateAllianceActivity"
            android:exported="false" /> <!-- Notifications Activity -->
        <activity
            android:name=".ui.notifications.NotificationsActivity"
            android:exported="false" />
        <activity
            android:name=".ui.shop.ShopActivity"
            android:exported="false" /> <!-- Firebase Messaging Service -->
        <activity
            android:name=".ui.model.InventoryActivity"
            android:exported="false" /> <!-- Firebase Messaging Service -->
        <service
            android:name=".service.MyFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service> <!-- Notification Listener Service -->
        <service
            android:name=".service.NotificationListenerService"
            android:enabled="true"
            android:exported="false" /> <!-- Default notification metadata -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@android:drawable/ic_dialog_info" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/primary" />
    </application>

</manifest>